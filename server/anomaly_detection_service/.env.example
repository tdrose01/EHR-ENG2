# PostgreSQL Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ehr-eng2
DB_USER=postgres
DB_PASSWORD=your_password_here

# Anomaly Detection Configuration
# Time window in hours to look back for new data
LOOKBACK_HOURS=24
# Z-score threshold for flagging an anomaly
Z_SCORE_THRESHOLD=3.0

# Logging Level
LOG_LEVEL=INFO
